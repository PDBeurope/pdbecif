stages:
  - test
  - build

tests:
  stage: test
  script:
    - echo "export PATCH=$CI_BUILD_ID" > PATCH_VERSION
    - apt-get update
    - apt-get install -y --force-yes lsb-release fakeroot
    - which python
    - which pip
    - which python2
    - which python3
    - pip install setuptools find_packages
    - python setup.py test
  artifacts:
    paths:
      - ./*.deb
  variables:
    PATCH: $CI_BUILD_ID
  only:
    - dev
    - master

